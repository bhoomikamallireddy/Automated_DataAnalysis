name: SonarCloud Analysis

on:
  push:
    branches:
      - main

  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  sonarqube:
    name: SonarCloud Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Necessary for SonarCloud to see commit history

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install backend dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run backend tests with coverage
        working-directory: backend
        env:
          SECRET_KEY: test-secret-key-for-ci-only-32-bytes-minimum
          DB_PASSWORD: test-db-password
          CORS_ALLOWED_ORIGINS: http://localhost:3000
          EMAIL_HOST_USER: test@example.com
          EMAIL_HOST_PASSWORD: test-email-password
          FRONTEND_URL: http://localhost:3000
        run: pytest --cov=. --cov-config=.coveragerc --cov-report=xml:coverage.xml --cov-report=term-missing

      - name: Normalize coverage paths for SonarCloud
        run: |
          sed -i 's#filename="api/#filename="backend/api/#g' backend/coverage.xml
          sed -i 's#filename="core/#filename="backend/core/#g' backend/coverage.xml

      - name: SonarCloud Scan
        uses: SonarSource/sonarqube-scan-action@v3
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
